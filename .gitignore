# Built zips and images are attached to releases, not committed.
dist/
*.zip

# Stock configs and dumps pulled off the headset. Meta's, and reference only;
# customize.sh regenerates from the device's own /vendor/etc.
work/

__pycache__/
*.pyc
.DS_Store
Thumbs.db

# Docs are being rewritten. Delete this block to start tracking them again.
README.md
CHANGELOG.md
RELEASE_NOTES.md

# Release builds are attached to Releases, not committed.
release/
